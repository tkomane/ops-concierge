# Moved: the live GitHub Actions workflow is `.github/workflows/pages.yml`.
# This file is kept as a pointer so older docs still resolve.
# Do not add a second deploy workflow here — Actions only loads `.github/workflows/`.
