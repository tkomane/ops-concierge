**Feature 003 correction assignment - complete integration before acceptance.**

Continue from `e2798c988c93beea97749f97811d8be30dcbdf9f`, after checking current HEAD and preserving unrelated work. Read `REVIEW-003.md` once. Codex independently ran the suite: 65 passed. The refusal fix and backend HTTP work are useful; the app still fails the existing acceptance gates.

Keep this within the remaining correction budget. The implementation lead should own the shared result/state integration. Do not start another redesign, new framework, scenario expansion or recursive worker round. Retain both stories and the Alexa+ simulation path. Pages correction, new video publication and Devpost Submit remain held.

1. **Preserve consent and the target plan.** In the browser, Doorstep -> neighbour unavailable -> `Approve plan_1` executes the new plan instead of rejecting the superseded one. Bedtime -> `Confirm whether Mira is available` executes both actions. `Approve only if the parcel is ours` is also classified as approval. Replace generic leading-word approval with bounded supported intent handling that carries an explicit plan ID and treats unresolved questions/conditions as clarification. Verify no writes for these cases; current unconditional approval executes once; repeats produce zero additional calls.

2. **Finish the result contract from HTTP to UI.** The actual client turns a structured HTTP 500 into null; `runTool` then executes a successful mock mutation. Preserve errors, execution source and stable operation identity. An attempted bridge mutation failure must stop and expose recovery. Pass the selected recipient/action/timing/plan through notification and task arguments. The backend currently returns a draft task while the UI announces confirmed; report only the status supported by the actual result. Test a successful notification followed by a failed task and safe retry without sending the notification twice.

3. **Make planning respond to facts.** Changed calendar responses currently leave the decision unchanged. `motion:false` and `parcelVisual:false` count as positive evidence. Mismatches and failed inspections still yield approvable drafts. Consume typed observations, respect false/unknown/error values, and require clarification where evidence is missing or inconsistent. Derive recipient and timing from usable household/calendar facts. `Mira is unavailable` in Bedtime must either produce a feasible changed plan or ask a precise clarification; it currently marks the neighbour unavailable and still chooses Mira. Update every visible plan summary and backup consistently.

4. **Repair new-story entry and persistence.** On a fresh screen, the `Start bedtime` suggestion claims to resume but performs no inspection and leaves nothing to approve. Resume only an existing usable session. After an acted Doorstep plan, reload currently retains the card but loses all messages and tool history: `syncSessionMessagesTools` writes a clone that `store.save()` immediately overwrites. Keep complete session state in one store, including evidence and per-operation execution state. Isolate results across stories. Keep plan/operation identities safe across reload. Verify first entry from buttons, suggestions and text, both switching directions, reload and fresh reset.

5. **Replace weak acceptance evidence.** Test the visible outcome, transcript and action count, not just story IDs, chat success phrases or internal status flags. Exercise failures at the fetch/HTTP boundary as well as the existing force-fail hook. Run the existing suite plus focused regression checks for items 1-4. Then complete desktop/390 x 844, themes, reduced motion, simulation marker and copy/print checks. The CSS still hides the header simulation badge below 900 px. Keep unsupported checks INCOMPLETE. Reconcile the task list and evidence table with actual results.

For reproduction, Codex supplied `probe-003.cjs` and `probe-003-results.json`. Run `node probe-003.cjs /path/to/ops-concierge`. This probes actual modules and the production client/runner with injected fetch responses; it is not a browser HTTP test and is not a replacement for one.

Return one concise pack at the final SHA: actual commands/results, all original acceptance cases marked PASS/PARTIAL/FAIL/INCOMPLETE, browser evidence for the regressions, changed tool-response evidence, action counts and HTTP failure recovery. Preserve the held external steps. Report usage if available; otherwise say unavailable. Do not claim complete acceptance until the app integration, not merely isolated modules, passes.
