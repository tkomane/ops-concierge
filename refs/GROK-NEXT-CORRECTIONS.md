**Continue Feature 003 corrections from `677a432`; acceptance is still blocked.**

Preserve the fixes that passed: fresh Bedtime suggestion, caregiver adaptation, explicit stale-plan rejection, reload history, story switching, repeated completed approval and the mobile simulation marker. Codex reran the suite: 65 passed. Read `REVIEW-CORRECTIONS.md` and the retained probe/results once. Check current HEAD and preserve unrelated work.

Keep this to one integration pass within the remaining credit budget. No new scenario, redesign, framework or recursive worker round. Pages correction, replacement video and Devpost Submit remain held.

1. **Bound consent by the entire utterance.** In the browser, `Go ahead if Mira confirms` immediately executes notification and task. Prefix branches still accept conditions after `go ahead`, `do it`, `yes, approve` and explicit plan IDs. Use a small complete allowlist/grammar of unconditional approvals, with current-plan target validation. Everything outside it should clarify without actions. Do not add only another exception for the exact repro phrase.

2. **Preserve uncertain execution and resume partial work.** Structured HTTP 500 is fixed, but fetch rejection after POST still returns null and becomes successful mock execution. Once a mutation may have been sent, return an attempted/unknown failure and block mock continuation. The actual approval coordinator also duplicates notifications: notify succeeds, task fails, retry yields notify=2/task=1 despite the no-resend promise. Persist per-plan operation progress and stable identities; retry only the unfinished operation, including after reload. Verify actual calls, not just counters or chat text.

3. **Finish both ends of the contract and render returned status.** The client now sends plan fields, but Python dispatch silently drops them. Actual bridge inspection results also lack the typed observations expected by the planner, so the ordinary Doorstep bridge fixture asks for clarification. Align mock, backend, client and planner schemas. Have the backend consume the selected recipient/action/timing and operation identity; prove changed inputs affect the returned result. The UI still copies `Status: confirmed` from a draft tool result because it never renders `artefactStatus`. Keep approval state distinct from execution/artifact state wherever they appear, and use returned identity/status in the card, copy and print paths.

4. **Close the evidence gate in both stories.** No-results Doorstep, false-event/no-order Doorstep, a failed order lookup, and all-failed Bedtime still produce approvable plans. Define minimum evidence per existing story; absent, negative, unknown or failed required inputs must stop approval and expose clarification/recovery. Cover these combinations with focused tests.

Run the supplied review probe against the corrected checkout, add focused regressions, rerun the existing suite, and verify the ordinary bridge path plus dropped-response and partial-retry behaviour through the integrated app. Then finish the already-required UX checks; keep unrun items INCOMPLETE. Return one evidence pack at the exact SHA with actual call counts and result/state evidence. No claim of full acceptance while these paths fail. Report usage if available, otherwise unavailable.
